<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyTrees1"
        tools:targetApi="31">

        <!-- RegisterActivity -->
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false" />
        <!-- LoginActivity is the entry point -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"/>

        <!-- Budget Planner Activity -->
        <activity
            android:name=".ui.BudgetPlannerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Expense History Activity -->
        <activity
            android:name=".ui.ExpenseHistoryActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Navigation Activity -->
        <activity
            android:name=".ui.NavigationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- DashboardActivity -->
        <activity
            android:name=".ui.DashboardActivity"
            android:exported="true" />

        <!-- AchievementsActivity -->
        <activity
            android:name=".ui.AchievementsActivity"
            android:exported="false" />

        <!-- CategoryActivity -->
        <activity
            android:name=".ui.CategoryActivity"
            android:exported="false" />

        <!-- GameActivity -->
        <activity
            android:name=".ui.GameActivity"
            android:exported="false" />

        <!-- LeaderboardActivity -->
        <activity
            android:name=".ui.LeaderboardActivity"
            android:exported="false" />

        <!-- ProfileActivity -->
        <activity
            android:name=".ui.ProfileActivity"
            android:exported="true" />

        <!-- ExpenseActivity -->
        <activity
            android:name=".ui.ExpenseActivity"
            android:exported="false" />

        <!-- FileProvider configuration for sharing PDFs-->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true"/>

        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:value="@xml/file_paths" />

    </application>
</manifest>